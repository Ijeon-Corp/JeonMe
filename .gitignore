# Node / Next.js
node_modules/
.next/
apps/web/.env
apps/web/.env.local

# Playwright (No.134) -- laporan HTML/hasil test lokal, bukan bagian dari
# source, jangan ikut ke repo.
apps/web/playwright-report/
apps/web/test-results/
apps/web/blob-report/

# Go
apps/api/bin/
apps/api/.env

# Root env (kredensial VPS/PSP)
.env

# OS / editor
.DS_Store
*.log
.vscode/
.idea/
.claude/

# Backup zip lokal (bukan bagian dari source, jangan ikut ke repo)
jeonme.zip

# Override compose lokal per developer (mis. remap port kalau bentrok dengan
# service native di mesin masing-masing) -- jangan ikut ke repo.
docker-compose.override.yml

# File lock Excel sementara (dibuat otomatis saat file dibuka) -- bukan
# bagian dari konten, jangan ikut ke repo.
docs/~$*.xlsx
